streamlit>=1.38.0
yfinance>=0.2.43
plotly>=5.23.0
pandas>=2.2.0
numpy>=1.26.0
anthropic>=0.34.0
pytz>=2024.1
python-dotenv>=1.0.1
feedparser>=6.0.11

# Optional live NSE/BSE data via the user's own Groww account (see
# src/groww_data.py). Safe to leave installed even without credentials --
# the app falls back to yfinance automatically when unconfigured.
growwapi>=1.0.0
pyotp>=2.9.0

# NOTE: mcp_server/india_stock_mcp.py (the Claude Desktop MCP server) has its
# own separate mcp_server/requirements.txt. It is intentionally NOT included
# here: it's not imported by app.py or anything under src/, and its `mcp`
# dependency requires Python >=3.10 and pulls in a heavy dependency chain
# (uvicorn, starlette, pydantic, etc.) that has no reason to be a required
# install for the Streamlit web app itself.
