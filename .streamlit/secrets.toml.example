# Copy this file to `.streamlit/secrets.toml` (which is git-ignored) and fill
# in your real values. Never commit real secrets to version control.
#
# You can also set these as plain environment variables instead of using
# this file -- the app checks both.

ANTHROPIC_API_KEY = "sk-ant-api03-fZx...VAAA"

# Optional: override the demo login credentials.
APP_USERNAME = "demo"
APP_PASSWORD = "stocksense2026"
